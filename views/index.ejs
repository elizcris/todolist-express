<%# embedded javascript %>
<!DOCTYPE html> <%# HTML declaration for the browser %>
<html lang="en"> <%# HTML opening tag, english language %>
<head> <%# head where information and metadata related to site is held %>
    <meta charset="UTF-8"> <%# character encoding indicating which character set is used %>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"> <%# tells Microsoft browsers which version of Internet Explorer to render the page as %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <%# sets the width of available viewing area to width of device screen %>
    <title>Document</title> <%# title displayed in the browser tab %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <%# link to fontawesome as a stylesheet giving access to the icon library %>
    <link rel="stylesheet" href="css/style.css"> <%# link to our css file 'style.css' in 'public/css' folder %>
</head> <%# closing head tag %>
<body> <%# typo %>
<!DOCTYPE html> <%# typo %>
<html lang="en"> <%# typo %>
    <head> <%# typo %>
        <meta charset="UTF-8"> <%# typo %>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"> <%# typo %>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> <%# typo %>
        <title>Document</title> <%# typo %>
    </head> <%# typo %>
<body> <%# main section for content displayed %>
    <h1>Todo List: </h1> <%# main heading %>
    <ul class="todoItems"> <%# unordered list, assign class of 'todoItems' %>
    <% for(let i=0; i < items.length; i++) {%> <%# ejs for loop %>
        <li class="item"> <%# list item with class 'item' %>
            <% if(items[i].completed === true) {%> <%# ejs for if an item in an array with 'completed' property is true %>
                <span class='completed'><%= items[i].thing %></span> <%# create a span with class 'completed' to display the name of the task %>
            <% }else{ %> <%# ejs for else clause of if/else statement %>
                <span><%= items[i].thing %></span> <%# display the name of the task, task will have a 'completed' property of false %>
            <% } %> <%# closing the else statement %>
            <span class='fa fa-trash'></span> <%# fontawesome trashcan icon %>
        </li> <%# closing tag for the list item %>
    <% } %> <%# closing the for loop %>
    </ul> <%# closing the unordered list %>

    <h2>Left to do: <%= left %></h2> <%# Heading level 2 using ejs to display how many tasks are left to complete %>

    <h2>Add A Todo:</h2> <%# Heading level 2 %>

    <form action="/addTodo" method="POST"> <%# create a form with the POST (create) method that will go to the route '/addToDo' %>
        <input type="text" placeholder="Thing To Do" name="todoItem"> <%# accepts text inputs with placeholder text of 'Thing To Do' with the name 'todoItem' which makes it easy to reference later %>
        <input type="submit"> <%# the form submit button%>
    </form> <%# closing tag of the form %>
    

    <script src='js/main.js'></script> <%# link to javascript file 'main.js' in 'public/js' folder %>
</body> <%# closing tag of the body %>
</html> <%# closing tag of html%>
